<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.psi.system.mapper.SysUserMapper">

    <resultMap id="UserWithDeptShopResultMap" type="com.psi.system.dto.SysUserDTO">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="avatar" property="avatar"/>
        <result column="dept_id" property="deptId"/>
        <result column="dept_name" property="deptName"/>
        <result column="shop_name" property="shopName"/>
        <result column="status" property="status"/>
    </resultMap>

    <select id="selectUserPageWithDeptAndShop" resultMap="UserWithDeptShopResultMap">
        SELECT 
            u.id,
            u.username,
            u.nickname,
            u.email,
            u.phone,
            u.avatar,
            u.dept_id,
            u.status,
            d.dept_name,
            s.shop_name
        FROM sys_user u
        LEFT JOIN sys_dept d ON d.id = u.dept_id AND d.del_flag = 0
        LEFT JOIN shop_info s ON s.id = d.shop_id AND s.del_flag = 0
        WHERE u.del_flag = 0
        <if test="username != null and username != ''">
            AND u.username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="nickname != null and nickname != ''">
            AND u.nickname LIKE CONCAT('%', #{nickname}, '%')
        </if>
        <if test="deptId != null">
            AND u.dept_id = #{deptId}
        </if>
        <if test="status != null">
            AND u.status = #{status}
        </if>
        ORDER BY u.create_time DESC
    </select>

</mapper>