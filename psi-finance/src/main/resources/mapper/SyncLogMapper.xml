<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.psi.finance.mapper.FinanceSyncLogMapper">

    <select id="selectByType" resultType="com.psi.finance.entity.FinanceSyncLogEntity">
        SELECT type, last_download_time AS lastTime
        FROM finance_sync_log
        WHERE type = #{type}
    </select>

    <update id="updateLastTime">
        UPDATE finance_sync_log
        SET last_download_time = #{lastTime}
        WHERE type = #{type}
    </update>

    <insert id="insert">
        INSERT INTO finance_sync_log (tenant_id, type, last_download_time)
        VALUES ('1', #{type}, #{lastTime})
    </insert>

</mapper>